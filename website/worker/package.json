{
  "name": "timeless-hadith-worker",
  "private": true,
  "version": "0.1.0",
  "description": "Cloudflare Worker: retrieval-augmented chat API for Timeless Hadith.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "secret:supabase-url": "wrangler secret put SUPABASE_URL",
    "secret:supabase-key": "wrangler secret put SUPABASE_SERVICE_ROLE_KEY",
    "secret:openai": "wrangler secret put OPENAI_API_KEY"
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  }
}
